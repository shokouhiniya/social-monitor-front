'use client';

// Reuse the same field reports view — in the future, filter by platform
import { FieldReportsView } from '../field-reports/view';

// ----------------------------------------------------------------------

export function TelegramFieldReportsView() {
  return <FieldReportsView />;
}
