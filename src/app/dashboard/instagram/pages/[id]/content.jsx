'use client';

import { PageProfileView } from 'src/sections/pages/profile-view';

// ----------------------------------------------------------------------

export function PageProfileContent({ id }) {
  return <PageProfileView id={id} />;
}
